# Frontend runtime target for the energy market API.
# Leave empty for same-origin deployments or local Vite proxying.
# Set to a fully-qualified origin when the API is hosted separately.
VITE_API_BASE_URL=

# Example:
# VITE_API_BASE_URL=https://api.energy-intelligence.example.com
