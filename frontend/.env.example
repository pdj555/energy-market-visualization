# Frontend API origin for split hosting.
# Leave empty for same-origin (Next.js rewrites to backend in dev).
NEXT_PUBLIC_API_BASE_URL=

# Dev proxy target (server-side rewrites only).
# API_PROXY_TARGET=http://localhost:8080
